{
  "name": "kripto-v2",
  "version": "0.1.7",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": { "dev": "node --env-file-if-exists=.env scripts/dev.mjs", "start": "node --env-file-if-exists=.env scripts/dev.mjs", "test": "node --test tests/*.test.mjs", "build": "node scripts/check.mjs" }
}
